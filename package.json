{
  "private": true,
  "scripts": {
    "build": "npx @tailwindcss/cli -i src/input.css -o assets/tailwind.css --minify",
    "dev": "npx @tailwindcss/cli -i src/input.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.0",
    "tailwindcss": "^4.2.0"
  }
}
